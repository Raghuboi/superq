# Server
PORT=3000
HOST=0.0.0.0

# Processing (10s delay per spec)
PROCESSING_DELAY_MS=10000

# Cache (in-memory LRU)
CACHE_MAX_SIZE=1000
CACHE_TTL_MS=3600000

# Logging
LOG_LEVEL=info

# Optional Redis (comment out for in-memory)
# REDIS_URL=redis://localhost:6379
